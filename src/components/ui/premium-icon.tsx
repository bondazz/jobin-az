import React from 'react';
interface PremiumIconProps {
  className?: string;
  size?: number;
}
const PremiumIcon = ({
  className = '',
  size = 16
}: PremiumIconProps) => {
  return;
};
export default PremiumIcon;